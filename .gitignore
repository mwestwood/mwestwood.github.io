_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
.bundle/
/vendor/
node_modules/
.DS_Store
*.gem
.bundle
.ruby-version
tmp/
Gemfile.lock
.claude/
__pycache__/

# Plaintext vocabulary source — kept local only. The encrypted pages it
# generates (teaching/vocabulary/*.md) ARE committed; the plaintext is not,
# matching the site's "never commit unencrypted content" rule.
_tools/vocab_data.py

# Plaintext Word Maze word bank — same rule as vocab_data.py above: the
# encrypted page it generates (teaching/vocabulary/maze.md) IS committed,
# the plaintext source is not.
_tools/maze_data.py
